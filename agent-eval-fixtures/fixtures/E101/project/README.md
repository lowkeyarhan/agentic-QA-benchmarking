# maestro-ios-wdio-stub

Minimal iOS WebdriverIO + Appium page-object stub for Maestro→WDIO translation evals.

Includes real `inspect_screen` reference snippets from production (duplicate `mfAddStock` labels). The agent must implement page-object methods using hierarchy evidence — not invented XCUITest types.

Prerequisites: macOS + Maestro CLI optional for this eval (reference JSON is source of truth).
