# Root cause analysis — completeDocumentUploadScreen

<!--
Read these files before writing:
- references/wdio-failure.log (shows failing method and wrong selector)
- references/inspect-upload-screen.json (SHOULD be current hierarchy truth)
- references/legacy-selector-map.json (stale — marked deprecated, do not treat as current)
- references/inspect-payment-screen.json (different screen — contains UploadButton but is NOT document-upload)
- references/appium-debug.log (driver noise — not selector evidence)

Identify:
1. The exact failing method name from the log
2. The wrong selector in use (~UploadButton)
3. The correct current selector from hierarchy (~SubmitUpload)
4. Which reference(s) must be rejected as stale/wrong-context evidence
5. One-sentence root cause that cites hierarchy evidence
6. One-line code fix (selector replacement)
-->

- **failingMethod:**
- **wrongSelector:**
- **correctSelectorFromHierarchy:**
- **staleEvidenceRejected:**
- **irrelevantEvidenceNotUsed:**
- **rootCause:**
- **proposedFix:**
