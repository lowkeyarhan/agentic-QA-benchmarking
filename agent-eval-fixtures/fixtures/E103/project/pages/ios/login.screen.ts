export class IosLoginScreen {
  // TODO: Reference files in references/ directory contain both iOS hierarchy and legacy Android-style mappings.
  // Use references/ios-login-inspect-snippet.json as current hierarchy truth.
  // references/ios-legacy-hierarchy.json is from an older build and uses retrofit-mapped labels.

  async tapLogin(): Promise<void> {
    throw new Error(
      "Not implemented — port using references/ios-login-inspect-snippet.json, not Android resource-id or legacy iOS labels",
    );
  }
}
