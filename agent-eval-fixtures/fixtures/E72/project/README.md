# captured-mobile-hierarchy

This fixture is self-contained. It does not require a live Android emulator.

Use `references/current-catalog-hierarchy.json` as the authoritative captured
Maestro hierarchy for the current SauceLabs MyDemoApp catalog screen.

`references/adb-quick-selectors.md` is archived context and intentionally
contains broad class-name selector traps.

Do not create Maestro YAML flows or project files for this selector-discovery
request.
